# Optional OpenFGA configuration file copied into the unikernel image.
# Uncomment and edit the fields below if you prefer file-based configuration
# instead of environment variables. Environment variables always win when both
# are provided.
#
# log:
#   format: json
#   level: info
#
# http:
#   addr: 0.0.0.0
#   port: 8080
#
# datastore:
#   engine: postgres
#   uri: postgres://openfga:password@postgres:5432/openfga?sslmode=disable
